export { default as LButton } from './button'

// import type { App, Plugin } from "vue";
// import LButton from "./button";

// // install方法第一个参数是vue的构造器，第二个参数是可选的选项对象
// LButton.install = (app: App) => {
//   app.component(LButton.name, LButton);
//   return app;
// };

// export default LButton as typeof LButton & Plugin;
